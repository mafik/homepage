Compile the `contents` directory and save results into the `build` directory.

`./homepage.py build`

Same as above + start a server on port 8000.

`./homepage.py serve`

Build the webpage & push it to 

`./homepage.py deploy`
